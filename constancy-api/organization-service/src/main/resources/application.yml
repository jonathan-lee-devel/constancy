constancy:
  front-end:
    url: http://localhost:4200
  keycloak:
    url: http://localhost:8080
  gateway:
    url: http://localhost:8081
  organization-service:
    url: http://localhost:8082
server:
  port: ${ORGANIZATION_SERVICE_PORT}
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/constancy-organization-service
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: constancy-java-client
            client-secret: CRqc9v3r0tIHS9EJKQ0288ZafesHvR2F
            authorization-grant-type: authorization_code
            scope: openid
        provider:
          keycloak:
            issuer-uri: ${constancy.keycloak.url}/realms/constancy
            user-name-attribute: preferred_username
      resourceserver:
        jwt:
          issuer-uri: ${constancy.keycloak.url}/realms/constancy
